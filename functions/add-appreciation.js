const { query } = require('./utilities/hasura')
exports.handler = async event => {
  // const { id, what, why, express, action } = event.body

  return {
    statusCode: 200,
    body: ''
  }
}

